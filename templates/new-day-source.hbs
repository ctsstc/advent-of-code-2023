import { getLines } from "../../utilities/files";

export async function SolveDay{{dayNumber}}Part{{partNumber}}(filePath: string): Promise<number> {
    const lines = await getLines(filePath);

    return 0;
}

